@model IEnumerable<QueryBuilder1._0.MAARITYS>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MAARITYS_EHTO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VALINTAOPERAATTORI.VALINTAOPERAATTORI1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MAARITYS_EHTO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VALINTAOPERAATTORI.VALINTAOPERAATTORI1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
