@model QueryBuilder1._0.kysely

@{
    ViewBag.Title = "Edit";
}

<h2>Muokkaa kyselyä</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4></h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.id)

        <div class="form-group">
            Kysely
            <div class="col-md-10">
                @Html.EditorFor(model => model.kysely1, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.kysely1, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Tallena muutokset" class="btn btn-secondary" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Takaisin", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
